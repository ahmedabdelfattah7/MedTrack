import 'package:flutter/material.dart';

class MedicalImages extends StatelessWidget {
  const MedicalImages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
